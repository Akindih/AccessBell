import os
import cv2
import psycopg2
import numpy as np
import face_recognition


# PostgreSQL connection
connection = psycopg2.connect(
    host="localhost",
    database="smart_doorbell",
    user="doorbelldara",
    password="doorbell19"
)
cursor = connection.cursor()

# Insert person into DB
def insert_person(full_name, relationship=None):
    cursor.execute("""
        INSERT INTO known_person (full_name, relationship)
        VALUES (%s, %s)
        RETURNING person_id;
    """, (full_name, relationship))
    person_id = cursor.fetchone()[0]
    connection.commit()
    return person_id

#  Insert encoding into DB
def insert_encoding(person_id, encoding):
    binary_encoding = encoding.tobytes()
    cursor.execute("""
        INSERT INTO face_encoding (person_id, encoding)
        VALUES (%s, %s);
    """, (person_id, binary_encoding))
    connection.commit()
try:
    f = open("data.csv", "r")
    print("data set opee=ne")
    f.close()
except FileNotFoundError:
        print("bad")
except Exception as e:
        print("error:" , e)# Process one person's folder
def process_person_folder(PERSON_NAME, folder_path):
    print(f"\nProcessing: {PERSON_NAME}")
    encodings = []

    for filename in os.listdir(folder_path):
        image_path = os.path.join(folder_path, filename)

        # Load image
        image = cv2.imread(image_path)
        if image is None:
            print(f"Skipping unreadable file: {filename}")
            continue

        # Convert BGR → RGB
        rgb = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

# python
import os
BASE_DIR = os.path.dirname(os.path.abspath(__file__))  # folder where the script lives
DATASET_DIR = os.path.join(BASE_DIR, "dataset")

if not os.path.isdir(DATASET_DIR):
    print(f"Dataset folder not found at `{DATASET_DIR}`")
    print(f"Current working directory: `{os.getcwd()}`")
    # optional: create the folder if you expect it to be created automatically
    # os.makedirs(DATASET_DIR, exist_ok=True)
    raise SystemExit(1)

for PERSON_NAME in os.listdir(DATASET_DIR):
    folder_path = os.path.join(DATASET_DIR, PERSON_NAME)
    if os.path.isdir(folder_path):
        process_person_folder(PERSON_NAME, folder_path)        # Detect face
        boxes = face_recognition.face_locations(rgb)
        if len(boxes) == 0:
            print(f"No face found in: {filename}")
            continue

        # Generate encoding
        encoding = face_recognition.face_encodings(rgb, boxes)[0]
        encodings.append(encoding)

    if len(encodings) == 0:
        print(f"No usable images for {PERSON_NAME}.")
        return

    # Insert person into DB
    person_id = insert_person(PERSON_NAME)

    # Insert all encodings
    for enc in encodings:
        insert_encoding(person_id, enc)

    print(f"Added {len(encodings)} encodings for {PERSON_NAME} (person_id={person_id})")


# Main: Loop through dataset folders
DATASET_DIR = "dataset"

for PERSON_NAME in os.listdir(DATASET_DIR):
    folder_path = os.path.join(DATASET_DIR, PERSON_NAME)
    if os.path.isdir(folder_path):
        process_person_folder(PERSON_NAME, folder_path)

# Close DB connection
cursor.close()
connection.close()
print("\nAll done. Encodings stored in PostgreSQL.")
